name: blog-test

# port of your docker container
port: 8081

variables:
    APP_PASSWORD: 1s2d456s2
    APP_NAME: blog-test

scripts:
    - chmod 777 -R tmp
    - chmod 777 -R logs
    - chmod 777 -R webroot/data
    - chmod +x bin/cake
